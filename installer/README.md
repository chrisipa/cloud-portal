# Cloud Portal Installer

## Prerequisites

### Install Ansible

* Add package repository:
```
sudo apt-add-repository ppa:ansible/ansible
sudo apt-get update
```

* Install ansible binary:
```
sudo apt-get install ansible
```

## Execute installer

* Call bash script:
```
./install.sh
```